# 网易云歌单归档 Web 应用实施方案

状态：已获准开发。独立于 Android 构建，Java 21 + Spring Boot 3.5、Vue 3、SQLite，单实例单账号、多歌单。

## 交付范围

1. 独立管理员登录，网易云 Cookie 导入与扫码登录、登录状态检查。
2. 输入歌单链接或 ID 订阅；默认每 15 分钟检查、首次全量，可仅监听新增；暂停与手动检查。
3. 读取完整 trackIds 并分批补全详情。完整快照和持久化下载任务在同一事务写入；失败不覆盖旧快照。
4. 以母带、Hi-Res、无损、极高、标准为高保真策略；环绕策略独立。记录请求与实际音质，拒绝试听，权限错误与网络错误分开处理。
5. SQLite 持久化队列，有限并发、重试、暂停/取消、重启恢复。歌单共享歌曲只下载一份，删歌保留文件。升级成功前保留原文件。
6. 文件完整性、格式、时长检查；歌词、封面、标签与 M3U8 歌单。媒体库浏览、手动补全及可选低频升级检查。
7. Dockerfile、Compose、配置与部署说明、关键自动化测试。

## 模块

- api：管理 REST API 和 Spring Security 会话/CSRF。
- netease：从当前项目移植 EAPI/WEAPI、会话、歌单和资源协议；不自动切换其他音乐来源。
- store：SQLite WAL，订阅、成员关系、歌曲、任务、文件、设置。
- worker：到期检查调度器和下载线程池。数据库为任务事实来源；重启将中断任务重新排队。
- media：同一音乐目录内写临时文件，校验后移动；使用 ffprobe 检测音频、ffmpeg 写标签（不重编码）。
- frontend：Vue 管理控制台，同源部署。

## 默认策略与边界

- 15 分钟检查、并发 2、首次全量、删除仅解除订阅关系、每 7 天可选升级复查。
- 音质以服务实际返回和文件检查为准；接口档位不等于文件真实采样规格。
- 网易云接口不是稳定的官方开放下载 API，真实账号扫码/权限与下载必须在目标环境验收；Cookie 导入提供替代登录入口。
- 服务器保存音乐，浏览器关闭后仍运行。单实例；SQLite 放本地盘，音乐可放 NAS。
- /data 保存数据库与独立密钥文件；/music 保存音乐及工作文件。管理员密码由环境变量提供，首次启动不提供公共默认密码。
- 保留源代码 GPLv3-or-later 版权声明和许可。

## 验证

单元/集成测试覆盖完整歌单判定、快照事务、跨歌单去重、仅新增、试听拒绝、实际档位、任务恢复、失败重试、暂停取消与下载完整性。前端构建并本地启动验证登录、CSRF 和 REST。Docker 若本机不可用，明确记录未执行容器验收。
